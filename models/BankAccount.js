


export class BankAccount {
  constructor(
    id,
    userId,
    name,
    description
  ) {
    this.id = id
    this.userId = userId
    this.name = name
    this.description = description
  }
}